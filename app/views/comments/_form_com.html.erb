<% if user_signed_in? %>
  <%= simple_form_for([movie, comment], data: {insert_in_list_target: "form", action: "submit->insert-in-list#send"}) do |f| %>
    <%= f.input :content, input_html:{class: "infor"}, label: false%>
    <%= f.submit "Post", class: "btn btn-danger" %>
  <% end %>
<% end %>
