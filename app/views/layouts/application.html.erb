<!DOCTYPE html>
<html>
  <head>

	    <%= favicon_link_tag asset_path('favicon-32x32.png') %>

    <title>CatchUpMovies</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@300&family=Libre+Baskerville&family=M+PLUS+Rounded+1c&family=Roboto+Slab:wght@500&family=Rubik:wght@700&family=Signika:wght@300&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <script src="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.js"></script>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.css" rel="stylesheet">

  </head>

  <body>
    <div class="row mx-0">
      <% unless current_page?(root_path) %>
        <div class="col-2 p-0">
            <%= render "shared/sidebar" %>
        </div>
      <% end %>
      <div class="col-10 mx-auto p-0">
        <div class="custom_container">
          <%= yield %>
        </div>
        <%= render "shared/flashes" %>
        <%= render 'shared/footer' %>
      </div>
    </div>
  </body>
</html>
